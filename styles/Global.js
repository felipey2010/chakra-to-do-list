import { chakra } from "@chakra-ui/react";

export const HomeTitle = chakra("text", {
  baseStyle: {
    fontSize: "5xl",
    textTransform: "uppercase",
    textAlign: "center",
    fontWeight: 700,
  },
});
